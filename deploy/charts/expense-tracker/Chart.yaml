apiVersion: v2
name: expense-tracker
description: Umbrella chart for Expense Tracker (backend, frontend, postgres)
type: application
version: 0.1.0

dependencies:
  - name: backend
    version: 0.1.0
    repository: "file://../backend"

  - name: frontend
    version: 0.1.0
    repository: "file://../frontend"

  - name: postgresql
    alias: postgres
    version: 18.2.3
    repository: "https://charts.bitnami.com/bitnami"
